import { CanvasType, PlayerType } from 'types';

export const canvas: CanvasType = {
  backgroundColor: '#333333',
  width: 800,
  height: 600,
};

export const player: PlayerType = {
  x: 400,
  y: 300,
  width: 10,
  height: 10,
  color: '#FFFF00',
};
